name: workflow_call_sample

on:
    workflow_call:
        inputs:
            REPO_NAME:
                type: string
                required: true

jobs:
    sample_job:
        runs-on: ubuntu-latest
        steps:
        - name: print input_var
          run: bash ${{inputs.REPO_NAME}}